[package]
  cjc-version = "1.0.5"
  name = "cui_images"
  description = "Static image studio: formats, fitting, cached decoding and per-view styles."
  version = "1.0.0"
  target-dir = ""
  output-type = "executable"
  compile-option = "-O2"
  override-compile-option = ""
  link-option = ""
  package-configuration = {}

[dependencies]
cui = { path = "../.." }
