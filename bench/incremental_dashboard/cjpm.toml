[package]
cjc-version = "1.0.5"
name = "bench_incremental_dashboard"
version = "0.1.0"
output-type = "executable"
description = "Real-renderer A/B benchmark for retained command replay and partial damage."

[dependencies]
cui = { path = "../.." }
bench_harness = { path = "../harness" }
