[package]
cjc-version = "1.0.5"
name = "bench_controls_form"
version = "0.1.0"
output-type = "executable"
description = "End-to-end (display) frame-rate benchmark: a controls-dense settings page (switches, radios, sliders)."

[dependencies]
cui = { path = "../.." }
bench_harness = { path = "../harness" }
bench_scenes = { path = "../scenes" }
